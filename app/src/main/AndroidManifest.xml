<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.davidmerino.davidmagicapp">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".view.app.App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">

        <activity
            android:name=".view.activity.MenuActivity"
            android:label="@string/app_name">
            <!-- Add the following lines to launcher activity -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".view.activity.LoginActivity" />
        <activity android:name=".view.activity.CardListActivity" />
        <activity android:name=".view.activity.DiceActivity" />
        <activity android:name=".view.activity.DetailCardActivity" />
        <activity android:name=".view.activity.BuyCardActivity" />
        <activity android:name=".view.activity.MockBoosterCardActivity" />
        <activity android:name=".view.activity.BoosterListActivity" />
        <activity android:name=".view.activity.DetailBoosterActivity" />
        <activity android:name=".view.activity.ShopMapActivity" />
        <activity android:name=".view.activity.ShopListActivity" />

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyBoazvoCj5IgSopH_0DBChncxjrZ35__f8" />
        <uses-library
            android:name="org.apache.http.legacy"
            android:required="false" />

    </application>
</manifest>
